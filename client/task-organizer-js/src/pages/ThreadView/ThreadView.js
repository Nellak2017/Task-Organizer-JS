import { SideNav, ThreadViewLarge } from "../../components";

const ThreadView = () => {
    return (
        <>
            <SideNav />
            <ThreadViewLarge />
        </>
    );
}

export default ThreadView;